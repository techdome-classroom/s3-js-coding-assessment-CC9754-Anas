/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function (s) {
  let comm = 0;
  let curl = 0;
  let bigb = 0;
  for (let i = 0; i < s.length; i++) {

  }
};

module.exports = { isValid };
